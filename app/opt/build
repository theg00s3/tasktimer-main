#!/bin/bash

SCRIPT_DIR=$(dirname `readlink -f $0`)
PROJECT_DIR=$(readlink -f $SCRIPT_DIR/../../)
if [ "$PROJECT_DIR" = "/" ]; then
  PROJECT_DIR="/pomodoro.cc"
fi

echo $PWD
echo $PROJECT_DIR
pwd
cd $PROJECT_DIR/app
pwd

npm run build

LAST_EXIT_CODE=$?

exit $LAST_EXIT_CODE
