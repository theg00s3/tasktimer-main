@import '../styles/vars.styl'
.navigation-bar
  float left
  width 100%

.brand
  display inline-block
  width 1.7em
  height @width
  background-size 100%
  background-image url("../assets/images/pomodoro.cc.png")


.early-access {
  // height: .9em;
  // line-height:
  margin-top: 0.26em;
  float: right;
  background: #5182d2;
  color: #fff !important;
  font-weight: 500;
  font-size: .9em;
  border-radius: 5px;
  padding: 0.15em 13px;
}
html body .early-access a {
  color: #fff !important;
}
.underline {
  text-decoration: underline;
}