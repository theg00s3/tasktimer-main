import {} from '../actions'

export const defaultState = {}

export default function user (state = defaultState, action) {
  switch (action.type) {
    default: {
      return defaultState
    }
  }
  // return state
}
