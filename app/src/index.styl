@import "../node_modules/normalize.css/normalize.css"

header_padding = .75em
header_height  = header_padding + 1em


header[role=header]
  position fixed
  top 0
  left 0
  padding header_padding
  height header_height
  border-bottom 1px solid black
